# Merchandise

Use `artwork` for editable designs, `mockups` for review images, `print-ready` for vendor exports, and `listings` for product titles, descriptions, sizes, prices, and fulfillment notes. Confirm mascot and logo usage before sending files to a print-on-demand vendor.

