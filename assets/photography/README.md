# Photography Library

Store original photography by build, system, harvest, founder, and product. Put corrected and cropped masters in `processed`. Keep capture dates and a short description in filenames or accompanying metadata where practical.

The existing mature-system image remains in `../photos` because it is already used by the live site.

