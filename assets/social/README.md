# Social Media Assets

Use `content-calendar` for publishing plans and one folder per channel for platform-ready creative. Keep reusable layouts in `templates`. Each campaign should retain its caption, CTA, destination URL, disclosure, publish date, source asset, and final export.

The primary channels are Instagram, Facebook, TikTok, YouTube, Pinterest, and LinkedIn.

